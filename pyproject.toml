[project]
name = "triton-monitor"
version = "1.0.0"
requires-python = ">= 3.11"

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
# La consigna pide explicitamente timezone.utc, no el alias datetime.UTC
ignore = ["UP017"]

[tool.pytest.ini_options]
testpaths = ["tests"]
